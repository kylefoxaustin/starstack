__pycache__/
*.pyc
# outputs — stacks are big, keep them out of the repo
*.tif
*.tiff
*.fit
*.fits
*.csv
starstack_*/
# but keep the small README example image
!docs/*.png

# packaging / test leftovers
build/
dist/
*.egg-info/
.pytest_cache/

# root-level previews / outputs from local runs
/*.png
